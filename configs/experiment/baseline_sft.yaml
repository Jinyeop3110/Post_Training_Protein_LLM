# @package _global_

defaults:
  - override /model: qwen3_4b
  - override /encoder: esm3_small
  - override /data: mol_instructions
  - override /training: sft_qlora

experiment_name: baseline_sft_${now:%Y%m%d_%H%M%S}

# Override specific settings
training:
  epochs: 3
  lr: 2e-4
